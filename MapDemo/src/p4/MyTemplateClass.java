package p4;

class Animal
{}

class Dog extends Animal{}

class DogType1 extends Dog{}
class DogType2 extends Dog{}


class Cat extends Animal{}

class Car{}

public class MyTemplateClass {
	
	public static void main(String[] args) {
		/*
		 * MyTemplate<Dog> doglist = new MyTemplate<>();
		 * 
		 * doglist.insertElement(new Dog()); doglist.insertElement(new Animal());
		 * doglist.insertElement(new DogType1());
		 * 
		 * Dog d = (Dog)doglist.getElement(2);
		 */
		
		/*
		 * MyTemplate<Car> stringList = new MyTemplate<>();
		 * 
		 * stringList.insertElement("A"); stringList.insertElement("A");
		 * stringList.insertElement("A"); stringList.insertElement("A");
		 * 
		 * String str = stringList.getElement(4);
		 */
		
		
	}

}
